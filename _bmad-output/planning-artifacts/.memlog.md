---
topic: Wax enrichment pass control
updated: 2026-08-14T12:05
---

- (decision) Update the incomplete legacy PRD in place as a focused brownfield PRD for Wax enrichment-pass control so BMAD epic creation has one authoritative PRD input.
- (direction) Wax must diarize transcriptions by default; this is currently observed not to happen.
- (direction) The canonical persistent settings path is XDG_CONFIG_HOME/wax/config.toml, normally ~/.config/wax/config.toml, with diarization as an initial supported setting.
- (direction) wax ep -l and --list enumerate enrichment passes with a numbered human-readable name and description.
- (direction) wax info -i and --item-id report applied and available enrichment passes for one Wax item.
- (direction) wax job -p and --enrichment-pass plus -i and --item-id enqueue an unapplied pass for asynchronous Wax processing.
- (decision) Treat diarization as a registered enrichment-pass capability with automatic application enabled by default, explicit item-level state, and a manual retry path.
- (constraint) An enabled diarization request may not silently publish success when runtime dependencies are missing; the unapplied or failed state must remain observable and retryable.
- (constraint) Enrichment-pass failures remain independent and must not block sibling passes, source-audio preservation, or safe archival.
- (decision) Preserve existing Wax CLI and EP commands while adding the requested shorthand and top-level info and job surfaces.
- (decision) Keep Phase 0A audio-path reconciliation separate from this Wax enrichment-control story; it will be distilled into its own bmad-spec contract.
- (assumption) This is a solo brownfield developer utility, so use the small-scope PRD shape and operational success measures rather than consumer journeys or market metrics.
- (event) PRD memlog audit complete: every live direction and constraint is represented in prd.md or addendum.md; retired legacy wrapper assumptions were not carried forward.
- (event) Input reconciliation complete against the user request, AGENTS.md, ENRICHMENT-PASSES.md, the architecture spine, and the migration plan; no load-bearing Wax enrichment requirement is missing.
- (event) PRD reviewer gate calibrated for a solo brownfield utility and run quietly; FR consequences, non-goals, CLI compatibility, and failure truthfulness are decision-ready with no phase blocker.
- (event) Configured structure and prose polish completed for LLM downstream readers; no substantive structural cut or communication fix was required.
- (event) PRD finalized on 2026-08-14.
- (change) Confirmed the live diarization runtime is a stale editable whisperlivekit install whose source package was removed in commit 1d21e8b; the failure is runtime integrity, not a false default.
- (constraint) A post-hoc Diarize EP needs a host-resolved source-audio input plus bounded atomic document-body mutation or an equivalent durable timed-segment contract; the current wax.ep.v1 surface cannot perform the requested transformation.
- (event) PRD and addendum re-reconciled and finalized after runtime and post-hoc transformation evidence was added.
- (constraint) Remove the waxd service WAX_DIARIZATION=1 pin when config support lands because it would permanently override a canonical transcription.diarize=false value.
- (constraint) Do not rely on the current dual-checkout coincidence: waxd runs from /home/delorenj/HeyMa while PATH resolves transcribe from /home/delorenj/code/HeyMa.
- (event) PRD and addendum finalized after runtime deployment reconciliation.
