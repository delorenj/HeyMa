---
topic: Wax Phase 0A audio-path reconciliation
updated: 2026-08-15T11:29
---

- (direction) Jarad requested a Phase 0A BMAD spec and authorized the orchestrator to resolve workflow choices without approval prompts.
- (event) Live read-only inspection on 2026-08-15 found Wax actively recording, waxd and Syncthing running, and Syncthing folder audio still mapped receiveonly to ~/HeyMa/inbox; no live mutation was attempted.
- (decision) Use slug wax-phase-0a-audio-path-reconciliation and treat this as a create operation distilled in express mode from rich architecture, migration, control-contract, project-instruction, code, and live-state inputs.
- (capability) CAP-1: Wax can grant a durable daemon-independent maintenance lease that blocks protected work during migration; success means idle-only acquire, observable renewal/release/expiry, and rejection across every start path even while waxd is stopped.
- (capability) CAP-2: Wax continuously imports stable cross-device audio from receive-only dropoff without mutating the feed; success means boot and runtime reconciliation, full-SHA deduplication, atomic no-replace publication, restart idempotency, and zero dropoff writes.
- (capability) CAP-3: An operator can execute and reverse the ownership cutover from evidence rather than prose; success means a durable step journal, before/after configuration and byte manifests, verified reverse operations, and no unrepresented preflight hash.
- (capability) CAP-4: A machine can prove the target ownership boundary before project instructions change; success means Syncthing receiveonly maps only to dropoff, Wax alone writes stream and inbox, and no receive-only directory receives a local write.
- (capability) CAP-5: Both ingest routes can be demonstrated exactly once after cutover; success means one cross-device drop is imported and processed once and one local recording appears atomically in inbox and is processed once.
- (constraint) Current AGENTS.md prohibition on local writes to ~/HeyMa/inbox remains binding until the Phase 0A exit gate passes and live configuration plus route proofs agree.
- (constraint) No Phase 0A step deletes, moves, overwrites, truncates, chmods, or otherwise mutates source recordings, transcripts, S3 identity, Wax state, Syncthing version history, credentials, models, or voice assets.
- (constraint) Cutover and rollback fail closed unless Wax is idle: no recording, finalizer, active queue item, importer publication, or concurrent Wax operation; unknown, stale, changing, malformed, or unreachable authority blocks mutation.
- (constraint) The importer treats dropoff as immutable: it ignores Syncthing temporaries and control directories, verifies source stability, copies through inbox-local durable staging, and never writes markers or completion state into dropoff.
- (constraint) Every migration mutation requires an active re-observed lease, matching expected Wax revision, and a durable journal entry with a verified reverse operation.
- (constraint) Path and workflow decisions use resolved absolute paths and current identifiers; Phase 0A must not disable or edit an unrelated n8n workflow that watches /home/delorenj/audio.
- (constraint) Automated implementation and acceptance tests use isolated WAX_ROOT and synthetic Syncthing fixtures; they never exercise destructive operations against the live home directories.
- (decision) Resolve the Phase 0A versus Phase 0B ordering cycle by implementing only the minimal Wax-local identity, epoch, generation, expected-revision, and token-free maintenance projection required to make leases safe; the full wax.status.v1 provider contract remains Phase 0B.
- (decision) Protected work under a Wax lease includes capture start and toggle-to-start, finalizer or recovery start, queue claim or worker start, importer publication, service restart, reconfiguration, and cutover mutation; stop, quiesce, inspection, status, and lease-owner operations remain available.
- (decision) If lease renewal fails or active ownership cannot be re-proved, finish no new mutation, record lease_lost, and leave the last verified byte-conserving state intact; continuation or rollback requires a fresh lease and complete preflight rather than mutating after expiry.
- (decision) Full-SHA deduplication preserves an existing Wax item and all transcript, backup, transition, and enrichment-pass projections; an existing hash records import provenance without creating pending work or repointing a still-valid path, while same-name different-content imports publish collision-safely as distinct items.
- (decision) The rollback journal records planned, started, applied, verified, and rolled_back states plus prior Syncthing folder configuration, service state, Wax revision, filesystem identifiers, hashes, and reverse commands; replay is idempotent and never removes preserved data.
- (decision) Importer readiness and isolated restart, stability, collision, deduplication, and source-immutability tests must pass before Syncthing is repointed to dropoff.
- (decision) The cutover keeps the prior inbox marker and version history recoverable, populates dropoff by copy rather than move, and changes AGENTS.md only after the machine ownership check and both route proofs pass.
- (constraint) A timestamped evidence bundle under _bmad-output/implementation-artifacts/heyma-control records preflight, journal, sanitized status, manifests, tests, cutover, rollback readiness, and final proofs; prose alone cannot satisfy an exit gate.
- (direction) Adopt AGENTS.md, ARCHITECTURE-SPINE.md, MIGRATION-PLAN.md, and CONTROL-CONTRACT.md as downstream companions rather than duplicating their load-bearing detail into the kernel.
- (note) The configured persistent fact project-context.md is absent, so no facts were available from that optional file; AGENTS.md and the ratified architecture documents supply project context.
- (direction) Non-goals are unified tray work, Phase 0B full provider schemas, repository extraction or product relocation, Wax diarization and EP CLI work, broad n8n cleanup, and deletion of legacy recovery surfaces.
- (event) Self-validation coherence PASS: SPEC.md satisfies all five kernel fields, five stable unique capabilities each contain intent and success, constraints bend design, non-goals bound scope, prose is lean, companion paths resolve, and no assumptions or open questions remain.
- (event) Self-validation preservation PASS: live ownership conflict, target paths, AD-17 through AD-19, importer invariants, lease protocol, idle and fail-closed gates, byte conservation, rollback surfaces, evidence bundle, n8n path safety, AGENTS update order, and both route proofs land in the kernel or named companions.
- (note) Wrapper-only content intentionally omitted from the kernel: BMAD routing ceremony, architecture review history, candidate-comparison prose, and document-generation metadata; adopted companions retain narrative rationale where downstream value remains.
- (event) Spec finalized on 2026-08-15 with SPEC.md, migration-invariants.md, and canonical memlog; Story Breakdown was not run because the request was for the Phase 0A contract and the user directed orchestration without approval prompts.
